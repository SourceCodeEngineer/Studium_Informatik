#include <stdio.h>              //inkludieren der standard bibliothek stdio.h was den input und output printf zulässt
#include <stdlib.h>             //inkludieren der standard bibliothek stdlib.h was das keywort "EXIT_SUCCESS" zulässt

int main(void) {                //main funktion definieren mit void (leere)
    printf("Hello World!\n");   //Ausgabe der Hello World funktion
    return EXIT_SUCCESS;        //rückgabe des wertes 0 was mit "EXIT_SUCCESS" über die Standardbibliothek stdlib.h passiert.
}
